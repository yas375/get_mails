Скрипт принимает на входе список доменов. А на выходе возвращает список email адресов, на которые зарегистрированы эти домены. Список будет в формате csv. И там выводится домен и email.

Поддерживаемые доменные зоны:
*.com
*.org
*.net
*.ru
*.su
*.info
*.biz
*.kiev.ua
*.com.ua
*.ua

Если в ходе работы скрипта он не смог обработать какую-то доменную зону, то он в конце об этом сообщит. Вобще в конце довольно подробная статистика выдаётся.

Требования для запуска
1. Операционная система linux.
2. php + apache
3. Должна быть установлена утилитка whois. Если её нету, то её можно установить командой:
 sudo apt-get install whois
4. На папке data должны быть права на запись. Сделать это можно командой:
chmod 777 data/
